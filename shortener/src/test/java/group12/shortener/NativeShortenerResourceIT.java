package group12.shortener;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeShortenerResourceIT extends ShortenerResourceTest {

    // Execute the same tests but in native mode.
}